#include<stdio.h>
int main()
{int a ,b ;
printf("enter number");
scanf("%d %d",&a,&b);
printf("the  increment is %d and %d is decrement",a++,b-- );
printf("the 2 increment is %d and %d is decrement",a++,b-- );
return 0;
}